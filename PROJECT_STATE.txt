P4KEY PROJECT STATE

updated utc
2026 08 28 00 10 27

project
ESP32-P4 2FA Key

current stage
04 CTAPHID transport

stage status
Prompt 03 is closed at its fifth pushed commit with the powered native unplug replug validation explicitly deferred and not passed Prompt 04 has two pushed commits and its preserved static transmit runtime generation reset CBOR stub keepalive cancel and isolated wait profile pass the focused 18 CTAPHID RX TX USB tests and normal firmware build before the third commit under the superseding four commit MVP scope

repository path
/home/ishan/esp32p4-2fa-key

branch
main

upstream
origin/main

local and upstream sync
synced through 9242f01 with zero ahead and zero behind before the third stage 04 commit

last successful commit
9242f01 build frame receive paths

BOARD

exact Waveshare model
ESP32-P4-Module-DEV-KIT

board revision
not established because no revision marking is visible in the available front photograph or official schematic

module
ESP32-P4-Module with ESP32-P4NRW32 32 MiB stacked PSRAM and 16 MiB Nor flash

ESP32 P4 chip revision
1.3 proven directly by esptool and the flashed baseline boot log

flash and PSRAM
16777216 flash bytes and 33554432 PSRAM bytes proven at runtime with PSRAM at 200 MHz

native USB device connector
Type-C connector labeled USB with the direct module USB1P1 data path reserved as the device candidate

native USB mode
Type C USB is FS PHY0 GPIO24 D minus GPIO25 D plus which defaults to USB Serial JTAG and is now reversibly routed to OTG1.1 before TinyUSB starts with healthy 303a 4004 Windows enumeration proven

logging connector
Type-C PWR USB TO UART through CH343P on GPIO37 and GPIO38 Windows COM7

button GPIO
35 BOOT Key2 candidate for user presence

button active level
active low from the schematic with internal pull up and runtime idle raw level 1

button fact source
official schematic Key2 GPIO35 to GND plus baseline boot sample idle level

USB VID
0x303a Espressif development VID permitted by the pinned component and not for product shipment

USB PID
0x4004 pinned component HID only development mapping and not for product shipment

TOOLCHAIN

ESP IDF
6.0.2 verified checkout 7101770dc6db2667b3c477cc31365dd1acd6db4e

Mbed TLS
4.1.0 public PSA Crypto API selected

target
esp32p4 pre v3 silicon path for the attached revision 1.3 device

esp tinyusb
2.2.1 pinned exactly with upstream TinyUSB 0.21.0~1 locked transitively

esp cbor
target 0.6.1~4 not added in stage 01

compiler
riscv32-esp-elf-gcc 15.2.0

host OS
Ubuntu under WSL2 Linux 6.6.87.2 x86_64 does not own the board USB while Windows loads healthy HID compliant fido nodes for 303a 4004 and an explicitly user approved elevated process proved raw FIDO HID access

host fido tool version
not installed

ROOT MODE

selected mode
development

development root status
not created

hardware HMAC slot
not selected

hardware HMAC purpose
not selected

irreversible provisioning
not performed

LAST SUCCESSFUL COMMANDS

firmware build
source /home/ishan/esp-idf/export.sh then scripts/build.sh passed after the final PHY route clock guard with total image size 236080 bytes and 77 percent app partition free SHA 256 b7cacbe36b768d9b5e900c5fd8dc62b4bcdd5f9d4886ee878c72ecd327eade52

USB bringup firmware build
clean builds from empty build/prompt03-clean-normal and build/prompt03-clean-bringup directories passed with total image sizes 236080 and 238684 bytes hashes 34165facafa79697fbb44cebe703150f34684c7c42665c4652a22f57fd14f704 and f202a9d05651e9df73afed2da8bdea38e83cd6c6a5aaf4e192c4f5598f1ae6d5

USB bringup target flash
Windows Python with esptool 5.3.1 wrote and hash verified only bootloader at 0x2000 partition table at 0x8000 and corrected bringup app at 0x10000 through the native USB Serial JTAG path using USB reset DIO 40 MHz 16 MB and hard reset without force or whole flash erase

USB bringup target boot
the bounded COM7 capture passed on ESP32 P4 revision 1.3 with IDF 6.0.2 flash 16 MB PSRAM 32 MB TinyUSB driver port 0 full speed HID ready and the fixed USB bringup exchange enabled

normal USB target flash and boot
Windows esptool 5.3.1 on COM7 wrote and hash verified only the final bootloader partition table and normal app without force or whole flash erase then UART proved revision 1.3 inside the v0.0 through v1.99 image range 16 MB flash 32 MB PSRAM TinyUSB full speed HID ready and no bringup task log

crypto target profile build
source /home/ishan/esp-idf/export.sh then scripts/build_crypto_test.sh passed with total image size 345100 bytes 67 percent app partition free and p4key.bin SHA 256 ed029db92ca0b3171bef9f11562b76b7cab4a197d70926baeb1abee7af2de783

host unit tests
python3 -m unittest discover -s tests/host -v passed 32 tests including 16 USB source queue PHY route policy and mocked HID checks

p256 pure helper checks
python3 -m unittest discover -s tests/host -v compiles p4_crypto_p256_util.c with Wall Wextra Werror and runs fixed scalar range and canonical DER cases

continuity check
python3 scripts/state_check.py passed

CTAPHID wire checks
python3 -m unittest tests.host.test_ctaphid_constants -v passed two verified constant documentation and compile checks

CTAPHID receive checks
python3 -m unittest tests.host.test_ctaphid_rx -v compiled the actual pure C receive channel and time sources with Wall Wextra Werror then passed all PING boundaries broadcast and retained INIT fragmented input spurious continuation exact and wrapped timeout busy silent CANCEL coherent queue full reset scrub bounded allocation and stale generation cases

CTAPHID transmit checks
python3 -m unittest tests.host.test_ctaphid_tx -v compiled the actual pure C send source with Wall Wextra Werror and passed exact 0 1 57 58 116 117 and 2048 byte framing reconstruction zero padding validation broadcast INIT and stopped emitter failure cases

CTAPHID runtime regression checks
python3 -m unittest tests.host.test_ctaphid_tx tests.host.test_ctaphid_rx tests.host.test_usb -v passed 18 tests after the reset epoch current connection atomic cancel and generation aware asynchronous output review fixes

CTAPHID synthetic wait build
source /home/ishan/esp-idf/export.sh then scripts/build_ctaphid_wait_test.sh passed with CONFIG_P4KEY_HID_WAIT_TEST enabled USB bringup disabled total image size 245940 bytes and p4key.bin SHA 256 015f4b98e375433898470374c42bb29cd94692d946d2d5b68eef85b01773a1ef

Prompt 04 firmware build
source /home/ishan/esp-idf/export.sh then idf.py -B build build and idf.py -B build size compiled the complete static CTAPHID runtime and normal CBOR invalid command stub for the ESP32 P4 pre v3 profile at total image size 245218 bytes p4key.bin SHA 256 16f3aa9c0a42dc007b5234e4840ac388164db4aac5d1618a2ebc003d3e32b129 with both HID wait and USB bringup disabled

crypto config check
python3 scripts/check_crypto_config.py passed 18 required and disabled generated release and target profile sdkconfig checks

on target tests
normal non forced COM7 flash verified the exact target profile image then all 23 crypto self tests and all 9 benchmarks passed on the attached ESP32 P4 revision 1.3

crypto proof report
python3 scripts/crypto_report.py --sdkconfig build/crypto-test/sdkconfig --log captures/crypto-selftest-com7.log --size captures/crypto-test-size.txt --image build/crypto-test/p4key.bin --output docs/generated/CRYPTO_REPORT.txt passed with report status PASS IDF total image size 345100 bytes and a matching boot ELF fingerprint

USB test
python3 scripts/usb_desc_check.py passed all source checks one user approved elevated Windows process proved exactly one 303a 4004 FIDO HID interface and the exact 64 byte bringup exchange then the final normal image passed the elevated 2000 millisecond no response probe

FIDO test
not attempted

manual browser test
not attempted

WHAT WORKS

repository and private origin audited
pinned ESP IDF 6.0.2 environment verified
connected CH343 interface identified as Windows COM7
existing NNUE firmware read only state captured before replacement
minimal C firmware clean builds for ESP32 P4 revision v1.3
normal flash writes and verifies without force or erase flash
baseline firmware boots without a crash loop
runtime reports 16 MiB flash 32 MiB PSRAM two cores and 360 MHz
default partition table provides NVS at 0x9000 size 24 KiB and a 1 MiB factory app
environment build flash monitor and continuity tools pass their narrow checks
active low active high and unconfigured button helper tests pass
state checker positive missing file and likely secret fixture tests pass
installed IDF source paths for TRNG SHA AES GCM general ECC and dedicated ECDSA are recorded
release configuration requires hardware SHA AES GCM and general ECC
dedicated eFuse ECDSA sign and verify paths are disabled
AES unsupported curve ECC and non AES GCM fallbacks are disabled
crypto_init restores the supported pre v3 entropy source before PSA initialization
rand_fill uses esp_fill_random without allocating or printing bytes
sha256_sum uses the PSA streaming SHA 256 route and preserves output on precondition failure
AES 256 GCM uses fixed 12 byte nonces and 16 byte tags through PSA multipart AEAD
GCM rejects output to input overlap and clears the plaintext destination on processing failure
P 256 generation derivation signing and verification wrappers compile against the public PSA API
P 256 scalar range and strict DER helpers pass their host checks
the P 256 wrappers require fixed scalar coordinate digest and DER bounds and clear temporary buffers
the opt in target profile passes RNG SHA 256 AES 256 GCM and general P 256 known answer negative smoke and alias tests
the direct public PSA one shot GCM known answer uses DMA capable internal buffers and passes through the pinned whole message P4 accelerator branch
target benchmarks report minimum microseconds and repetitions only as supporting data
normal firmware keeps crypto self tests and benchmarks disabled
no RSA primitive or accelerator path is used because the v0.1 protocol requires ES256 only
the report parser rejects missing failed duplicated reordered or malformed target evidence and wrong benchmark repetitions
docs/generated/CRYPTO_REPORT.txt records only whitelisted facts and explicitly states proof limits
esp_tinyusb 2.2.1 and its transitive TinyUSB source are pinned by the manifest and lock file
the installed initialization descriptor HID callback PHY endpoint and task APIs are recorded in docs/USB_API_NOTES.txt
the source controlled report descriptor is exactly 34 bytes with the required FIDO usages one collection two 64 byte reports and no report ID
the full speed configuration descriptor is exactly 41 bytes with one class 03 subclass 00 protocol 00 interface and interrupt endpoints 0x01 and 0x81 at 64 bytes interval 5
the device identity is the documented development pair 303a 4004 with no USB serial and its non shipment status is recorded
the clean descriptor build selects one HID interface and disables every other TinyUSB device class
the normal application starts esp_tinyusb on the explicit full speed port only after crypto initialization
the receive callback checks interface report ID type and exact 64 byte length then performs one bounded copy without blocking allocation crypto storage button work parsing or packet logging
the fixed receive ring has eight generation tagged slots and rejects the newest report on full without overwriting unread data
detach marks USB unavailable increments the generation empties the ring invalidates an in hand reply and wakes blocked receive and send paths
suspend conservatively advances the same connectivity generation because separate UART power remains present and no native connector VBUS monitor GPIO is routed
usb_take and usb_send use one finite overall deadline and reject disconnect timeout or a response from an older connectivity generation
the statically allocated bringup task responds only to one exact nonsecret 64 byte request and is absent with both request and response strings from the normal image
the pure Python source checker requires the exact 34 report bytes 41 configuration bytes one HID and no other USB class while live reconstructed descriptors must match all FIDO semantics
the host suite compiles and runs the actual C depth eight ring including reject newest overflow FIFO wrap empty and reset behavior
usb_find asks HIDAPI only for the selected VID PID and rejects exposed mismatching interface or usage facts before opening a candidate
hid_probe requires an explicit bringup or normal no response mode validates the selected report descriptor before writing a host only report ID prefix plus exactly 64 bytes reads exactly 64 with a finite timeout and closes on every exit
hidapi 0.15.0 is the sole pinned host dependency and is loaded only by explicit live modes
the exact bringup firmware normally flashed and booted on the attached revision 1.3 board without force whole flash erase or irreversible configuration
UART logs remain available on PWR USB TO UART while the native device connector is handled separately
the board helper reversibly routes OTG1.1 USB WRAP to Type C FS PHY0 before TinyUSB starts and sets GPIO24 and GPIO25 to the drive capability required by the installed USB PHY implementation
the route regression fails when the selector pins strength or pre install order is removed and the repaired image makes Windows replace USB Serial JTAG with healthy 303a 4004 HID nodes
the elevated filtered Windows HID checks prove VID 303a PID 4004 one FIDO application collection the exact live 34 byte report descriptor one HID interface no other USB class two 64 byte interrupt endpoints and the exact 64 byte bringup round trip
the source and clean normal binaries contain no private bringup request response or enabled warning while the clean bringup binary contains all three expected strings
the final normal image flashed and booted on the attached revision 1.3 target with the pre v3 range and PHY0 clock invariant preserved
the normal target ignored the exact private bringup request for the full finite timeout and the host closed the HID handle
the conditional wrong report length target case is unavailable because Windows HIDAPI enforces the live 64 byte report shape before USB transfer and no counter pass is claimed
the pinned CTAP 2.2 USB HID binding was read before defining transport constants
CTAPHID command constants retain the full initialization packet wire byte including bit seven
verified HID errors INIT layout protocol version capability values keepalive states and CTAP cancel status have compile time host checks
the 2048 byte assembled message bound 500 millisecond local partial timeout and 100 millisecond keepalive cadence are documented without presenting the local timeout as a normative constant
four fixed channel records allocate only nonzero nonbroadcast hardware random CIDs with bounded collision retries
one active transaction and two fixed 2048 byte buffers provide an ownership handoff without heap allocation or unread message overwrite
the depth one complete handoff retains its own CID command length and operation generation independent from mutable channel parsing state
input parsing copies only declared remaining bytes ignores padding rejects oversize bad channel bad command bad length wrong sequence and conflicting work then clears failed partial state
spurious continuations are ignored and periodic cleanup leaves a bounded timeout marker for the next related continuation error
same CID INIT clears partial processing cancel and queued request state then returns the retained CID while broadcast INIT can allocate without disturbing an active channel
each claimed complete request carries its channel operation generation so a stale consumer cannot cancel finish or wipe newer same CID work after INIT resynchronization
valid CANCEL only marks the active CBOR operation and produces no direct response action
the complete host suite passes 35 tests after the receive core addition
transmit framing serializes every report of one logical response and passes every required payload boundary without heap allocation
the normal runtime uses one static transport task one static stub task one fixed complete handoff and a core then whole message transmit lock order
each application claim binds CID channel operation generation USB connection generation command and owner task
every attach detach and suspend changes the USB connection token and asynchronous output rejects a stale token before and during finite endpoint waits
an explicit reset request stops an in flight fragmenter while a reset epoch rejects a report taken across the reset boundary
disconnect resets all channels partial input complete handoff cancel state and application claims then wakes a blocked consumer
the final CBOR send chooses CTAP2 keepalive cancel under the same lock as response transmission so a processed CANCEL cannot race a normal body
same keepalive status is limited to the selected 100 millisecond cadence while a changed status may be sent immediately
the exact synthetic wait request profile is opt in unavailable with bringup and absent from the normal firmware image
the user moved the only data cable to Type C USB and Windows currently enumerates healthy 303a 4004 FIDO HID nodes while COM7 is intentionally unavailable

WHAT IS BROKEN

WSL does not own the native USB device and Windows requires elevation for raw access to the FIDO HID collection
the powered unplug replug without reboot evidence remains pending
the powered unplug replug counter gate cannot be proven with only one data capable cable and is explicitly deferred not passed and carried into the final acceptance checklist
physical PCB revision is not established
physical BOOT pressed level is not sampled yet
idf.py size emits a nonfatal sram_seg classification warning on the first v3 build and not on the clean pre v3 report

OPEN FACTS

physical board revision from rear silkscreen or packaging
physical BOOT key pressed level confirmation
negotiated speed is unavailable through the installed Windows HID and PnP tools while the selected live OTG1.1 PHY0 controller and maintained descriptor are full speed only

EXACT NEXT ACTION

commit the reviewed runtime and send work as add channel and send logic push fetch and confirm exact origin synchronization then add only the minimal host proof needed for live INIT short and 117 byte PING and CBOR invalid command before finish ctaphid mvp

MANUAL ACTIONS STILL REQUIRED

later provide a second known data capable USB C cable for the deferred powered native unplug replug and UART counter validation which remains not passed
inspect the board rear or package for a PCB revision marking
press BOOT during a later level diagnostic to directly confirm raw level 0
later perform website registration and login during the prompt that requests it

SHELLCHECK

not installed test not attempted and no package was installed

NOTES

never put a key nonce private scalar decrypted credential record or eFuse secret in this file
the user removed repository planning copies in published commit 61e622a and attached prompt pack files are the stage instruction source
Prompt 02 and Prompt 03 are complete under the one named deferred physical exception and Prompt 04 is in progress
the original Prompt 04 matrix and five commit plan are superseded by the user supplied four commit MVP scope exhaustive fuzz stress synthetic live wait and powered detach reconnect are not Prompt 04 completion gates
