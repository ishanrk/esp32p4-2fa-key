P4KEY PROJECT STATE

updated utc
2026 08 26 04 58 38

project
ESP32-P4 2FA Key

current stage
03 USB FIDO HID enumeration

stage status
in progress the bounded generation tagged USB report path and opt in fixed bringup exchange pass normal and bringup pre v3 builds with host inspection tools next

repository path
/home/ishan/esp32p4-2fa-key

branch
main

upstream
origin/main

local and upstream sync
synced through f5c0cd1 with zero ahead and zero behind before the third stage 03 commit

last successful commit
f5c0cd1 add fido hid descriptors

BOARD

exact Waveshare model
ESP32-P4-Module-DEV-KIT

board revision
not established because no revision marking is visible in the available front photograph or official schematic

module
ESP32-P4-Module with ESP32-P4NRW32 32 MiB stacked PSRAM and 16 MiB Nor flash

ESP32 P4 chip revision
1.3 proven directly by esptool and the flashed baseline boot log

flash and PSRAM
16777216 flash bytes and 33554432 PSRAM bytes proven at runtime with PSRAM at 200 MHz

native USB device connector
Type-C connector labeled USB with the direct module USB1P1 data path reserved as the device candidate

native USB mode
the required Type-C USB1P1 route is the ESP32 P4 full speed controller and must explicitly override the component high speed default actual enumeration remains pending

logging connector
Type-C PWR USB TO UART through CH343P on GPIO37 and GPIO38 Windows COM7

button GPIO
35 BOOT Key2 candidate for user presence

button active level
active low from the schematic with internal pull up and runtime idle raw level 1

button fact source
official schematic Key2 GPIO35 to GND plus baseline boot sample idle level

USB VID
0x303a Espressif development VID permitted by the pinned component and not for product shipment

USB PID
0x4004 pinned component HID only development mapping and not for product shipment

TOOLCHAIN

ESP IDF
6.0.2 verified checkout 7101770dc6db2667b3c477cc31365dd1acd6db4e

Mbed TLS
4.1.0 public PSA Crypto API selected

target
esp32p4 pre v3 silicon path for the attached revision 1.3 device

esp tinyusb
2.2.1 pinned exactly with upstream TinyUSB 0.21.0~1 locked transitively

esp cbor
target 0.6.1~4 not added in stage 01

compiler
riscv32-esp-elf-gcc 15.2.0

host OS
Ubuntu under WSL2 Linux 6.6.87.2 x86_64 currently sees no passed through board USB device while Windows sees the CH343 on COM7

host fido tool version
not installed

ROOT MODE

selected mode
development

development root status
not created

hardware HMAC slot
not selected

hardware HMAC purpose
not selected

irreversible provisioning
not performed

LAST SUCCESSFUL COMMANDS

firmware build
source /home/ishan/esp-idf/export.sh then scripts/build.sh passed with total image size 235524 bytes and 78 percent app partition free SHA 256 d7ab3331c0c33be06b5768523aa75f9569d339507751274e3754ab2bc847824b

USB bringup firmware build
source /home/ishan/esp-idf/export.sh then scripts/build_usb_bringup.sh passed with total image size 238128 bytes and 77 percent app partition free SHA 256 3cd76f7a9a3a9b01c7fc0d7cd392ff63c337cdcc47edbaff2c5c299eb3246e00

crypto target profile build
source /home/ishan/esp-idf/export.sh then scripts/build_crypto_test.sh passed with total image size 345100 bytes 67 percent app partition free and p4key.bin SHA 256 ed029db92ca0b3171bef9f11562b76b7cab4a197d70926baeb1abee7af2de783

host unit tests
python3 -m unittest discover -s tests/host -v passed 16 tests including 11 strict crypto report fixtures

p256 pure helper checks
python3 -m unittest discover -s tests/host -v compiles p4_crypto_p256_util.c with Wall Wextra Werror and runs fixed scalar range and canonical DER cases

continuity check
python3 scripts/state_check.py passed

crypto config check
python3 scripts/check_crypto_config.py passed 18 required and disabled generated release and target profile sdkconfig checks

on target tests
normal non forced COM7 flash verified the exact target profile image then all 23 crypto self tests and all 9 benchmarks passed on the attached ESP32 P4 revision 1.3

crypto proof report
python3 scripts/crypto_report.py --sdkconfig build/crypto-test/sdkconfig --log captures/crypto-selftest-com7.log --size captures/crypto-test-size.txt --image build/crypto-test/p4key.bin --output docs/generated/CRYPTO_REPORT.txt passed with report status PASS IDF total image size 345100 bytes and a matching boot ELF fingerprint

USB test
the source report descriptor exact byte check passed at 34 bytes both clean configurations have one HID class with other device classes disabled the ring compiles on the host with Wall Wextra Werror and strings proves the fixed exchange is present only in the bringup image target enumeration is not attempted yet

FIDO test
not attempted

manual browser test
not attempted

WHAT WORKS

repository and private origin audited
pinned ESP IDF 6.0.2 environment verified
connected CH343 interface identified as Windows COM7
existing NNUE firmware read only state captured before replacement
minimal C firmware clean builds for ESP32 P4 revision v1.3
normal flash writes and verifies without force or erase flash
baseline firmware boots without a crash loop
runtime reports 16 MiB flash 32 MiB PSRAM two cores and 360 MHz
default partition table provides NVS at 0x9000 size 24 KiB and a 1 MiB factory app
environment build flash monitor and continuity tools pass their narrow checks
active low active high and unconfigured button helper tests pass
state checker positive missing file and likely secret fixture tests pass
installed IDF source paths for TRNG SHA AES GCM general ECC and dedicated ECDSA are recorded
release configuration requires hardware SHA AES GCM and general ECC
dedicated eFuse ECDSA sign and verify paths are disabled
AES unsupported curve ECC and non AES GCM fallbacks are disabled
crypto_init restores the supported pre v3 entropy source before PSA initialization
rand_fill uses esp_fill_random without allocating or printing bytes
sha256_sum uses the PSA streaming SHA 256 route and preserves output on precondition failure
AES 256 GCM uses fixed 12 byte nonces and 16 byte tags through PSA multipart AEAD
GCM rejects output to input overlap and clears the plaintext destination on processing failure
P 256 generation derivation signing and verification wrappers compile against the public PSA API
P 256 scalar range and strict DER helpers pass their host checks
the P 256 wrappers require fixed scalar coordinate digest and DER bounds and clear temporary buffers
the opt in target profile passes RNG SHA 256 AES 256 GCM and general P 256 known answer negative smoke and alias tests
the direct public PSA one shot GCM known answer uses DMA capable internal buffers and passes through the pinned whole message P4 accelerator branch
target benchmarks report minimum microseconds and repetitions only as supporting data
normal firmware keeps crypto self tests and benchmarks disabled
no RSA primitive or accelerator path is used because the v0.1 protocol requires ES256 only
the report parser rejects missing failed duplicated reordered or malformed target evidence and wrong benchmark repetitions
docs/generated/CRYPTO_REPORT.txt records only whitelisted facts and explicitly states proof limits
esp_tinyusb 2.2.1 and its transitive TinyUSB source are pinned by the manifest and lock file
the installed initialization descriptor HID callback PHY endpoint and task APIs are recorded in docs/USB_API_NOTES.txt
the source controlled report descriptor is exactly 34 bytes with the required FIDO usages one collection two 64 byte reports and no report ID
the full speed configuration descriptor is exactly 41 bytes with one class 03 subclass 00 protocol 00 interface and interrupt endpoints 0x01 and 0x81 at 64 bytes interval 5
the device identity is the documented development pair 303a 4004 with no USB serial and its non shipment status is recorded
the clean descriptor build selects one HID interface and disables every other TinyUSB device class
the normal application starts esp_tinyusb on the explicit full speed port only after crypto initialization
the receive callback checks interface report ID type and exact 64 byte length then performs one bounded copy without blocking allocation crypto storage button work parsing or packet logging
the fixed receive ring has eight generation tagged slots and rejects the newest report on full without overwriting unread data
detach marks USB unavailable increments the generation empties the ring invalidates an in hand reply and wakes blocked receive and send paths
suspend conservatively advances the same connectivity generation because separate UART power remains present and no native connector VBUS monitor GPIO is routed
usb_take and usb_send use one finite overall deadline and reject disconnect timeout or a response from an older connectivity generation
the statically allocated bringup task responds only to one exact nonsecret 64 byte request and is absent with both request and response strings from the normal image

WHAT IS BROKEN

WSL currently receives neither board cable because the Windows USB devices are not attached through USB IP
the USB host inspection scripts source policy tests and target enumeration evidence are not implemented yet
physical PCB revision is not established
physical BOOT pressed level is not sampled yet
idf.py size emits a nonfatal sram_seg classification warning on the first v3 build and not on the clean pre v3 report

OPEN FACTS

physical board revision from rear silkscreen or packaging
physical BOOT key pressed level confirmation
native USB controller enumeration and negotiated speed in its later stage

EXACT NEXT ACTION

add strict source descriptor configuration queue and mocked HID host checks plus explicit discovery descriptor and probe tools without touching hardware

MANUAL ACTIONS STILL REQUIRED

inspect the board rear or package for a PCB revision marking
press BOOT during a later level diagnostic to directly confirm raw level 0
later perform website registration and login during the prompt that requests it

SHELLCHECK

not installed test not attempted and no package was installed

NOTES

never put a key nonce private scalar decrypted credential record or eFuse secret in this file
the user removed repository planning copies in published commit 61e622a and attached prompt pack files are the stage instruction source
Prompt 02 is complete Prompt 03 is in progress and Prompt 04 has not been started
