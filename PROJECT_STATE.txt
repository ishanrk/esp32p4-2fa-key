P4KEY PROJECT STATE

updated utc
2026 08 26 02 10 33

project
ESP32-P4 2FA Key

current stage
01 repository and board baseline

stage status
in progress host baseline tests pass

repository path
/home/ishan/esp32p4-2fa-key

branch
main

upstream
origin/main

local and upstream sync
synced through 57901f6 before the host tests commit

last successful commit
57901f6 add build and state tools

BOARD

exact Waveshare model
ESP32-P4-Module-DEV-KIT

board revision
unknown no revision marking is visible in the available board photo or official schematic

module
ESP32-P4-Module with ESP32-P4NRW32 32 MB stacked PSRAM and 16 MB Nor flash

ESP32 P4 chip revision
1.3 from the prior NNUE boot record direct confirmation in this firmware pending

flash and PSRAM
16 MB Nor flash configured and 32 MB stacked PSRAM configured at 200 MHz runtime confirmation pending

native USB device connector
Type-C connector labeled USB direct module USB1P1 path candidate for device mode

native USB mode
native USB controller exact runtime mode not yet tested

logging connector
Type-C PWR USB TO UART through CH343P on GPIO37 and GPIO38 Windows COM7

button GPIO
35 BOOT key candidate for user presence

button active level
active low runtime internal pull up required

button fact source
official Waveshare ESP32-P4-Module-DEV-KIT schematic Key2 GPIO35 to GND

USB VID
unknown for project device stage

USB PID
unknown for project device stage

TOOLCHAIN

ESP IDF
6.0.2 verified checkout 7101770dc6db2667b3c477cc31365dd1acd6db4e

target
esp32p4

esp tinyusb
target 2.2.1 not added in stage 01

esp cbor
target 0.6.1~4 not added in stage 01

compiler
riscv32-esp-elf-gcc 15.2.0

host OS
Ubuntu under WSL2 Linux 6.6.87.2 x86_64

host fido tool version
not installed

ROOT MODE

selected mode
development

development root status
not created

hardware HMAC slot
not selected

hardware HMAC purpose
not selected

irreversible provisioning
not performed

LAST SUCCESSFUL COMMANDS

build
. /home/ishan/esp-idf/export.sh then scripts/build.sh passed

host unit tests
python3 -m unittest discover -s tests/host -v passed 4 tests

on target tests
Windows System.IO.Ports read only NNUE protocol queries on COM7 succeeded

USB test
not attempted native USB cable not visible in WSL

FIDO test
not attempted

manual browser test
not attempted

WHAT WORKS

repository and origin audited
pinned ESP IDF 6.0.2 environment verified
connected CH343 interface identified as Windows COM7
existing NNUE firmware read only state captured before replacement
minimal C firmware builds for esp32p4
16 MB flash PSRAM and UART console settings are generated as intended
default partition table provides NVS at 0x9000 size 24 KiB and a 1 MiB factory app
environment build flash monitor and continuity tools pass their narrow checks
active low active high and unconfigured button helper tests pass
state checker positive missing file and likely secret fixture tests pass

WHAT IS BROKEN

WSL cannot access the attached CH343 serial interface directly
board revision is not established
idf.py size emits a nonfatal sram_seg classification warning

OPEN FACTS

physical board revision
direct boot confirmation of chip revision flash and PSRAM
native USB controller mapping and enumeration
physical BOOT key level test

EXACT NEXT ACTION

commit the host tests then perform the reversible target flash boot and button checks

MANUAL ACTIONS STILL REQUIRED

none before local project setup
USB pass through or a Windows flashing path is required for the target run
later perform website registration and login

SHELLCHECK

not installed test not attempted

NOTES

never put a key nonce private scalar decrypted credential record or eFuse secret in this file
